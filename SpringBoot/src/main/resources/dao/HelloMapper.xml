<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="it.hww.dao.HelloMapper">



<select id="queryOne" resultType="it.hww.service.entity.Employee">
    select * from  employee where eid = #{id,jdbcType=VARCHAR}
</select>
</mapper>